export interface Internship {
  id: number;
  name: string;
  link: string;
  category: string;
  faculty: string;
}

export interface PostInternship {
  name: string;
  link: string;
  category: string;
  faculty: string;
}

export interface UpdateInternship {
  name: string;
  link: string;
  category: string;
  faculty: string;
}
